{{ include('exercice2/product/header.html.twig') }}
{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.title) }}
    {{ form_row(form.content) }}

    <button type="submit">Submit</button>
    {{ form_end(form) }}
{% endblock %}