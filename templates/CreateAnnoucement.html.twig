{% extends 'base.html.twig' %}

{% block body %}
    {{ include('exercice2/product/header.html.twig') }}
    {{ form_start(CreateAnnoucementform) }}

    {{ form_row(CreateAnnoucementform.title) }}
    {{ form_row(CreateAnnoucementform.price) }}
    {{ form_row(CreateAnnoucementform.content) }}

    <button type="submit">Submit</button>
    {{ form_end(CreateAnnoucementform) }}
{% endblock %}